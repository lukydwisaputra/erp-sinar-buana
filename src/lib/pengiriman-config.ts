/**
 * Stand-in for EP-00 (Konfigurasi) email/SMTP account settings, which hasn't been
 * built yet. Kirim Email stays correctly disabled until that module exists and this
 * flips to true.
 */
export const pengirimanConfig = {
  emailTerkonfigurasi: false,
};
